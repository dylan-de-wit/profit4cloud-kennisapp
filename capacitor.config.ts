import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'profit4cloud-kennisapp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
